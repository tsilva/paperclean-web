# Keep resolution policy project-local so workstation settings cannot leak into uv.lock.
exclude-newer = "7 days"
constraint-dependencies = [
  "pytest>=9.0.3",
  "mistralai!=2.4.6",
  "guardrails-ai!=0.10.1",
]
